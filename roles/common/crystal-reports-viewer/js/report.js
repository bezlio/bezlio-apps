define(function () {
 
    function RunQuery (bezl, queryName) {

        switch (queryName) {

            default:
                break;
        }
    }

    return {
        runQuery: RunQuery
    }
});